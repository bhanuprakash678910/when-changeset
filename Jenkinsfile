pipeline {
    agent any
    stages {
        stage("Build") {
           /* when {changeset "**/*.java" }
              */
            }
      steps {
                echo "Hello World changeset"
            }
        }
    }
}
